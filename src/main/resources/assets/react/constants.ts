// This file contain some constants duplicated from /lib/explorer/constants
// It's needed because /lib/explorer is not available at compile time,
// nor at runtime on the client-side.

export const DEFAULT_INTERFACE_FIELDS = [{
	name: '_alltext',
	boost: 1
}];
